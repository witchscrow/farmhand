apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging
nameSuffix: -staging

resources:
  - ../../base/api
  - ../../base/queue
  - ingress.yaml

images:
  - name: api
  - name: queue

labels:
  - includeSelectors: true
    pairs:
      environment: staging
